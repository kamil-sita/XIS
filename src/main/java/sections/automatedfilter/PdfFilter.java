package sections.automatedfilter;

import java.io.File;

public class PdfFilter {
    public static void filter(File pdffile) {
        //PDDocument document = PDDocument.loadNonSeq(pdffile, null);
    }
}
